# =================================================================
# File .gitignore untuk Proyek Monorepo (FE, BE) dengan Docker
# =================================================================

# 1. Dependensi
# Mengabaikan node_modules di setiap sub-folder secara spesifik
/fe/node_modules
/be/node_modules

# 2. Build Artifacts & Output
# Spesifik untuk Next.js di folder frontend
/fe/.next
/fe/out
/fe/build
/fe/*.tsbuildinfo
/fe/next-env.d.ts

# 3. File yang Berhubungan dengan Docker
# Mengabaikan folder data database yang dibuat oleh Docker Compose
postgres_data/

# 4. Data Rahasia (PENTING)
# Abaikan file .env spesifik, tapi jangan pakai wildcard '*'
# Ini memungkinkan kita untuk menambahkan .env.example nanti.
.env
.env.local
.env.development
.env.production

# 5. File Log Debugging
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# 6. File Sistem Operasi & Editor
.DS_Store
*.pem

# 7. File Deployment (jika ada)
.vercel